\documentclass[tikz, border=10pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{trees, arrows.meta, positioning, decorations.pathreplacing}

\begin{document}

\begin{tikzpicture}[
  level distance=10mm,
  level 1/.style={sibling distance=80mm},
  level 2/.style={sibling distance=40mm},
  level 3/.style={sibling distance=20mm},
  level 4/.style={sibling distance=10mm},
  every node/.style={draw, circle, minimum size=5pt, inner sep=0pt}
]

% --- Binary tree with named leaves ---
\node (root) {}
  child { node {}
    child { node {}
      child { node (L) {} }   % leftmost leaf
      child { node {} }
    }
    child { node {}
      child { node {} }
      child { node {} }
    }
  }
  child { node {}
    child { node {}
      child { node {} }
      child { node {} }
    }
    child { node {}
      child { node {} }
      child { node (R) {} }   % rightmost leaf
    }
  };

% --- Height annotation ---
\draw[<->, thick] ([xshift=10mm]root.north) -- 
  node[right, midway] {$\log_2(n)$} 
  ([xshift=10mm]L.south);

% --- Width annotation ---
\draw[<->, thick] (L.south west) ++(0,-5mm) --
  node[below, midway] {$n$}
  (R.south east);

\end{tikzpicture}

\end{document}
