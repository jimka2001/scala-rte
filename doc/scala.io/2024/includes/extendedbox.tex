%% dont re-indent this file
\begin{lstlisting}[style=scalaioScala]
// one or more
re.+ == re ++ re.*

// optional, zero or one
re.? == EmptySeq | re

// exponent, n-times
re^4 == re ++ re ++ re ++ re
\end{lstlisting}
