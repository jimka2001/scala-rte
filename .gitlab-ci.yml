# this .gitlab-ci.yml was generated by chatGPT.
# https://chatgpt.com/share/69eccb6e-05ac-83eb-b580-60c936aca116

stages:
  - docker
  - test

variables:
  build_docker: 'true'

# to rebuild the docker image, you must change the docker/Dockerfile (add comment etc.)
docker-build:
  stage: docker
  image: docker
  script:
    - docker login -u gitlab-ci-token -p $CI_JOB_TOKEN $CI_REGISTRY
    - docker build --pull -f docker/Dockerfile -t $CI_REGISTRY_IMAGE:latest .
    - docker push $CI_REGISTRY_IMAGE:latest
  only:
    changes:
      - docker/Dockerfile
    variables:
      - $build_docker =~ /true/i

unit-scala:
  stage: test
  image: $CI_REGISTRY_IMAGE:latest
  timeout: 2h
  only: 
    - master
  script: 
    - sbt sbtVersion
    - ./bin/auto-test-scala-on-push.csh

